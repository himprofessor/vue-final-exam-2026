# TaskFlow Vue Final Exam - TODO

## Step 1 (Frontend)
- [x] Implement `createTask`, `updateTask`, `deleteTask`, `setFilters`, `resetFilters` in `frontend/src/stores/tasks.ts` following the `stores/categories.ts` pattern (loading/error handling + re-fetch after CRUD).


## Step 2 (Verification)
- [ ] Run backend + frontend and verify Tasks page create/edit/delete and filtering works.

